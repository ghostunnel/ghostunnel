language: go

sudo: false

env:
  - GO15VENDOREXPERIMENT=1

matrix:
  fast_finish: true
  allow_failures:
    - go: tip

go:
  - 1.5
  - tip

before_script:
  - go vet .

before_install:
  - python --version
  - openssl version
  - go get github.com/Masterminds/glide

install:
  - make build 

script:
  - make test
